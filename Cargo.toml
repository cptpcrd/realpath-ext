[package]
name = "realpath-ext"
version = "0.1.0"
edition = "2018"

authors = ["cptpcrd"]

[features]
default = ["std"]

std = []

[dependencies]
libc = { version = "0.2", default-features = false }
bitflags = "1.2"

[dev-dependencies]
criterion = "0.3"

[[bench]]
name = "bench"
harness = false
